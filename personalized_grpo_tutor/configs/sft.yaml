extends: base.yaml

sft:
  output_subdir: sft_qwen7b_lora
  num_train_epochs: 1
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 2.0e-4
  logging_steps: 5
  save_steps: 100
  warmup_ratio: 0.03
  max_seq_length: 2048

lora:
  r: 32
  alpha: 64

paths:
  sft_data: /home/liyongqi/project/personalized_grpo_tutor/data/process/apps_train_sft_stepwise.jsonl

sft_online_eval:
  enabled: true
  every_n_steps: 20
  num_samples: 6
  max_new_tokens: 256
